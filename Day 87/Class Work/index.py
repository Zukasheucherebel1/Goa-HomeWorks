def square_sum(numbers):
    res = 0
    for num in numbers:
        res = res + num*num
        return res
    
    #def transpose(matrix):
  #  list1 = []
   # list2 = []
    #for i in matrix:
     #   x = len(i)
     #   for y in i:
      #      list1.append(y)
       #     for a in range(0,x):
        #        list2.append(list1[n::x])
         #       
          #      return list2
            



          #  def smaller(arr):
    #a = []
    #counter = 0 
    #for j in range (len(arr)):
    #    j = arr[0]
     #   for k in arr:
      #      if j > k:
       #         counter += 1
        #        a.append(counter)
         #      arr.remove(arr[0])
          #      return(a)
          